#include <stdio.h>
void main()
{
    int i,n;
    int max;
    printf("enter the number of elment in the array: ");
    scanf("%d",&n);
    int arr[n];
    printf("\nenter tha element in the array; ");
    for(i=0;i<n;i++)
    {
        printf("\nenter element %d:",i+1);
        scanf("%d",&arr[i]);
    }
    max=arr[0];
    for(i=0;i<n;i++)
    {
        if(arr[i]>max)
        {
            max=arr[i];
        }
    }
    printf("\nThe largest number is %d",max);
}
**************************************************************************
OUTPUT
enter the number of elment in the array: 6

enter tha element in the array; 
enter element 1:345

enter element 2:987

enter element 3:08

enter element 4:419

enter element 5:654

enter element 6:989

The largest number is 989

=== Code Exited With Errors ===
